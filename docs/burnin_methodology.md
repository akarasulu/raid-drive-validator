# Burn-in methodology

The methodology intentionally layers firmware-level tests, media stress, latency screening, and transport-path checks. The destructive phase is used specifically to force weak sectors to surface before RAID deployment.
